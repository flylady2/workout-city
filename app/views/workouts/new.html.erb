<%= form_for [@trainer, @workout] do |f| %>

 <%= f.hidden_field :trainer_id %>
 
   <%= f.label :name %>
   <%= f.text_field :name %>

   <%= f.label :date %>
   <%= f.date_field :date %>



   <%= f.submit "Create a Workout" %>

<% end %>
